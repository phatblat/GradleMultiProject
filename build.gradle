allprojects {
    task showProjectDir {
        doLast {
            println projectDir
        }
    }
}

task removeBatchFile(type: Delete) { delete 'gradlew.bat' }

wrapper {
    gradleVersion = '4.9'
    distributionType = 'ALL'
    finalizedBy removeBatchFile
}
